package org.ych.art.utils.utils;

import java.math.BigDecimal;

public interface Defined {
    // 十进制 真
    BigDecimal D_TRUE = new BigDecimal(1);

    // 十进制 假
    BigDecimal D_FALSE = new BigDecimal(0);

    // 十进制 0
    BigDecimal D_ZERO = new BigDecimal(0);

    // 十进制 1
    BigDecimal D_ONE = new BigDecimal(1);

    // 十进制 -1
    BigDecimal D_BONE = new BigDecimal(-1);
}
